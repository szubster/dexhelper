## Scribe Journal

- Added JSDoc comments to `parseGen2NarrativeFlags` and `getUpcomingGen2Boss` in `src/engine/saveParser/utils/gen2EventFlags.ts` to explain their purpose, arguments, and return values.
- Highlighted the architectural quirk that for Gen 2 rival events, the flag being set means they are NO LONGER present because they have been defeated.
