# Scribe Log

- Added JSDoc for exported types `GameVersion`, `PokemonInstance`, and `SaveData` in `src/engine/saveParser/index.ts`.
- Added usage `@example` to `parseSaveFile` JSDoc in `src/engine/saveParser/index.ts`.
- Memory constraint: When writing scratch Node.js scripts via bash in a repository configured with `"type": "module"`, they must use the `.cjs` extension.
