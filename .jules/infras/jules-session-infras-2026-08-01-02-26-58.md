# Infras Journal - Session 2026-08-01-02-26-58\n\n## Critical Learnings\n- **Tooling configuration context:** Successfully added `commitlint` (with `@commitlint/config-conventional`) to the project.\n- **Integration:** Integrated `commitlint` into the existing `lefthook.yml` using the `commit-msg` hook, passing `{1}` to read the commit message file. This ensures all new commits adhere to conventional commit messages without adding significant complexity to the existing CI setup.
