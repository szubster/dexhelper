## 2026-09-25 - [Accepted] - Prompt improvement - Clean up Agile Coach session API and journal paths
**Type:** Prompt improvement
**Outcome:** Merged
**Why:** The `.github/agents/agile_coach.md` schedule contained an obsolete directive instructing the persona to use `getSessionActivities` from `.github/scripts/session-api.ts` to scan session activity transcripts via the Jules v1alpha API. `session-api.ts` does not export `getSessionActivities` and the v1alpha endpoint is obsolete, causing potential runtime errors or confusion when the Agile Coach runs. Additionally, the prompt referenced wildcard journal paths (`.foundry/journals/*`, `.jules/*`) rather than the canonical aggregated `master.md` journal files (`.foundry/journals/*/master.md`, `.jules/*/master.md`), which contradicts the system-wide journal aggregation structure maintained by the Archivist.
**Pattern:** Scrub agent persona prompts to remove references to obsolete or removed script APIs and align journal inspection instructions with the canonical `master.md` paths.
