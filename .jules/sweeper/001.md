# Sweeper Journal

Removed unused utility functions in `src/engine/saveParser/utils/hiddenItems.ts` alongside its test suite and type definition `HiddenItemData` in `src/db/schema.ts`.
Be careful with `pnpm knip --production`. It frequently flags entry points, CLI scripts, data generators, and active components as unused. Always manually verify dead code before deleting it, rather than blindly following tool output.
