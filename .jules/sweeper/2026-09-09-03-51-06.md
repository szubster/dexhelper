# Sweeper Session

## Actions
Deleted unused interface `InjectedPromptContext` from `.github/scripts/schema.ts` and unused constants from `src/engine/saveParser/parsers/gen2.ts`.

## Learnings
When using tools like `knip` to identify dead code, always verify findings with a global search tool like `grep` before removal, as they might be used implicitly in contexts that static analysis doesn't fully capture.
