## 🧹 Technical Debt Cleanup: Unused Benchmarks & API Exports

### 🎯 What
Removed dead benchmark code (`benchmarks/`, `tests/benchmarks/`), the associated `test:bench` package script, and an unused API export `dispatchJulesSession` in `.github/scripts/session-api.ts`.

### 💡 Why
- `knip` reported `dispatchJulesSession` as an unused export.
- `knip` reported `benchmarks/configs/esbuild.js` and other payload/test files as unused.
- A global `grep` revealed that `tests/benchmarks/run-benchmarks.ts` was only referenced by the `test:bench` script in `package.json`, which was no longer used elsewhere. The benchmarks were essentially abandoned dead code contributing to repository clutter.

### ✅ Verification
- Ran `pnpm knip` to verify no more unused exports or files (related to these changes).
- Ran `pnpm lint` and `pnpm test` to ensure no regressions were introduced.

### ✨ Result
Cleaner repository structure, smaller `package.json`, and resolved `knip` warnings, improving overall codebase health.
