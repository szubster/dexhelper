## Knip Cleanups
When cleaning up technical debt or dead code with `knip`, it's common to find magic numbers and old constants from early save parsing reverse engineering that have been exported but never used outside the file. They can be safely deleted.
Additionally, configuration files may have ignore rules for files that no longer exist, these also should be scrubbed when knip reports "Configuration hints".