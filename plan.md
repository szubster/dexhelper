1. Address PR comment: "Start week on Monday. Not on Sunday."
   - Update `DAYS_OF_WEEK` array in `src/components/Gen3RTC/Gen3RTCControls.tsx` to start on Monday: `['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday', 'Sunday']`.

2. Pre-commit
   - Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.

3. Submit the changes
   - Use `submit` with `feat-gen3-rtc-override` branch and reply to PR comments with `reply_to_pr_comments`.
