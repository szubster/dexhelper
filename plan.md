1. **Create the E2E Story**
   - Create `.foundry/stories/story-070-358-orchestrator-strict-completion-e2e.md` for E2E testing of the orchestrator's strict completion checks.
   - Use `owner_persona: tech_lead`, status `PENDING`, tags `orchestrator`, `architecture`, `e2e`, parent `epic-045-070-orchestrator-strict-completion`.

2. **Update Epic Markdown Body**
   - Update `.foundry/epics/epic-045-070-orchestrator-strict-completion.md` to check off the `- [ ] story-070-358-orchestrator-strict-completion-e2e` checkbox.

3. **Complete pre-commit steps**
   - Complete pre-commit steps to make sure proper testing, verifications, reviews and reflections are done.

4. **Submit changes**
   - Use the `submit` tool to finalize the empty PR as per the policy (this is an orchestrator architecture epic).
