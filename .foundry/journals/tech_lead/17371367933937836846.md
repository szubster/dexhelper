# Session 17371367933937836846

## Architectural Notes
- When drafting technical blueprints for save file parsing, the coder MUST catch `DataView` `RangeError` from out-of-bounds reads and throw a new Error with the message "The save file is corrupted or incomplete."
- Gen 3 save file parsing requires explicitly calculating relative memory offsets using the resolved section offset (e.g., `section1Offset`) instead of hardcoded absolute offsets to properly support A/B bank flash memory.
- All memory offsets, lengths, bit locations, and shifts must be defined as reusable constants at the module level.
- Created `task-333-346-gen3-roamer-extraction-tests-impl` to replace the permanently failed `task-333-333-gen3-roamer-extraction-tests-impl` due to missing constraints, ensuring the coder satisfies all architectural rules. Designated coder self-verification since unit tests are a low-risk verification task in themselves.
