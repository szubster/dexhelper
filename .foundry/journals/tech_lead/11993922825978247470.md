# Late-Binding Orchestrator Demotion Compliance
When a parent node has pending child tasks in active directories (even if their YAML says COMPLETED), submit an empty PR without checking the boxes to allow the orchestrator to demote it to PENDING.