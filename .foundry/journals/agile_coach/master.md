# Agile_coach Master Journal



---

## Aggregated from 2026-08-05-12-00-00.md

## 2026-08-05 - Accepted - Consolidate Redundancy in bolt.md
**Type:** Prompt improvement
**Outcome:** Submitted
**Why:** `.github/agents/bolt.md` contained hardcoded YAML frontmatter instructions (which violated schema constraints by requiring `rejection_count` and `rejection_reason` for IDEA nodes) and duplicated the `ls -1` sorting logic for sequence numbering. This caused prompt bloat and broke the DRY principle mandated by the Agile Coach's directive to consolidate redundancy and reference centralized documents.
**Pattern:** Proactively replaced redundant schema and file naming instructions with explicit references to `.foundry/docs/schema.md` and `.foundry/docs/knowledge_base/agents/core_policies.md` to prevent prompt rot and ensure system-wide consistency.


<!-- Merged from 2026-08-24-02-27-00.md -->
# Agile Coach Journal - 2026-08-24

## Consolidation of Redundancy
I identified that the "Orchestrator Safeguard (E2E/Integration Requirement)" was duplicated across `.github/agents/epic_planner.md`, `.github/agents/story_owner.md`, and `.foundry/docs/knowledge_base/agents/core_policies.md`. Additionally, the "Two-Tasks-Max Anti-pattern" was duplicated in `.github/agents/tech_lead.md`. To prevent prompt rot and ensure consistency, I removed these duplicated definitions from the persona prompts and replaced them with direct references to `.foundry/docs/knowledge_base/agents/core_policies.md`.

## New Core Policies Based on Historical Friction
1. **Missing Context & Task Suspension**: Added a new rule to `core_policies.md` addressing a friction point observed in the Coder's journal regarding missing memory offsets (e.g., Gen 3 PC Box extraction). The policy strictly requires agents to avoid guessing when technical context is missing. Instead, they must fail/suspend the current task and generate a new `RESEARCH` node dependency to find the missing information.
2. **Empty PR Automated Review False Negatives**: Added a note to the `Empty PR Checkbox Policy` in `core_policies.md` advising agents to ignore false negatives generated by automated code review tools when the patch only contains markdown checkbox updates. This addresses failures reported in the Coder's and Tech Lead's journals.

## Evolving Personas
1. **Researcher Artifact Cleanup**: Added an `Artifact Cleanup` responsibility to `.github/agents/researcher.md`. This rule explicitly requires the researcher to delete temporary files, directories, and build artifacts (and clear them from the git cache) when researching external codebases to prevent repository pollution, a recurring rejection reason found in the Researcher's journal.

<!-- Merged from 15466324859776924451.md -->
# Session 15466324859776924451

## Refactoring Redundancies in Persona Files

- **Consolidate Redundancy in coder, tech_lead, epic_planner, and story_owner**: Identified and removed explicit rules regarding DAG dependency mappings, two-tasks-max anti-patterns, orchestrator safeguards (E2E Integration Requirement), and autonomous execution logic from their respective base persona definitions. These constraints are already comprehensively defined inside `.foundry/docs/knowledge_base/agents/core_policies.md` and compiled dynamically at runtime by the Foundry Orchestrator. Consolidating this redundancy prevents prompt rot and bloat.