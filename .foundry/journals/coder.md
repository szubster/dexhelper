# Coder Journal

- I implemented the automated extraction of `research_references` using a standalone script `.github/scripts/foundry-resolve-research.ts` which is executed in `.github/workflows/foundry-engine.yml`. It works by parsing the frontmatter using `gray-matter` to avoid bash injection vulnerabilities.

- I implemented the automated extraction of `research_references` using a standalone script `.github/scripts/foundry-resolve-research.ts` which is executed in `.github/workflows/foundry-engine.yml`. It works by parsing the frontmatter using `gray-matter` to avoid bash injection vulnerabilities.
