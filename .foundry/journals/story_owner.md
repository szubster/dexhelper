- **2026-05-XX Pattern Insight:** When implementing sequential stories (e.g., in `epic-017-029-dag-dashboard-ui`), sibling dependencies should be correctly linked using `depends_on`. For instance, `story-029-051-implement-core-graph-visualization` correctly blocks on `story-029-048-evaluate-graph-libraries`. I also made sure to append the newly spawned story to the parent epic's markdown and tick the checkbox, without modifying its YAML frontmatter.

- Learning: When creating subsequent stories for an epic where a previous story has FAILED, ensure the new story depends on the FAILED story to block execution until the failure is resolved.

## 2026-05-18: Gen3 Data Formats
Note that generation 3 data formatting uses `.jsonl` for source files in the repository for ease of review, which is then compiled into MsgPack (`msgpackr`) via a Vite plugin for runtime use. This reduces dataset sizes and improves parse speeds (ADR 010).
- Sibling dependencies must strictly use node IDs (e.g., task-123-slug) instead of relative file paths to satisfy memory constraints.
- **2026-05-19 Pattern Insight:** When an implementation leaf task fails permanently and triggers the creation of a `RESEARCH` node and new retry tasks, do NOT update the originally associated pending `QA` task node frontmatter. Just update the QA task Markdown body indicating that it is CANCELLED and replaced by the new tasks.

- **2026-05-22 Pattern Insight:** When handling a permanently failed child node (e.g., reaching Max Rejection Count), we must NOT change the YAML frontmatter of associated orphaned pending QA task nodes. We should only append a `CANCELLED` notice in the orphaned node's markdown body. The `status` in the YAML frontmatter MUST remain whatever it previously was (e.g., `PENDING`). However, in the parent story's Markdown body, the checkbox for that cancelled task *must* still be ticked off (e.g. `[x]`). ADR 007 dictates that any unchecked boxes will prevent the parent story from being COMPLETED, regardless of whether the task was superseded or cancelled. Therefore, *all* tasks, including replaced/cancelled ones, must be checked in the parent's generated tasks list to allow DAG progression.
- **2026-05-23 Pattern Insight:** The story nodes (`story-032-062`, `063`, `064`) were unexpectedly generated ahead of time (violating the late-binding rule) and their `owner_persona` was incorrectly set to `story_owner` instead of `tech_lead`.
- **Pattern Insight:** When a child node permanently fails, a new `RESEARCH` node and follow-up `TASK` nodes must be spawned to investigate and fix the root cause. The orphaned `QA` task MUST NOT have its YAML frontmatter modified; instead, we only update its Markdown body to indicate it is CANCELLED. All parent story generated task checkboxes (including failed/orphaned ones) must be ticked to allow DAG progression.
- **2026-05-28 Pattern Insight:** When acting as the story_owner and observing that downstream nodes (like STORY nodes generated ahead of time) have the wrong `owner_persona` (e.g., `story_owner` instead of `tech_lead`), I must correct them to ensure smooth pipeline execution, while still strictly adhering to the rule of not modifying the target node's YAML frontmatter. I can update the markdown checkboxes of the downstream nodes to properly track completion.

- **2026-06-09**: Discovered a potential architectural/planning duplication issue while processing `epic-038-061-mirage-island-engine` (child of `prd-069-038-mirage-island-predictor`). There is another active PRD, `prd-068-038-mirage-island-data-extraction`, that aims to accomplish the exact same Gen 3 Mirage Island prediction logic (it has spawned parallel epics like `epic-038-061-mirage-island-save-parsing`). This overlap indicates a breakdown in the initial intake/PRD deduplication pipeline. Future planning should ensure `IDEA` nodes and their resulting PRDs are correctly merged if they address identical scopes to prevent parallel agents from implementing duplicate engines.
- **2026-06-09 Pattern Insight:** Breaking down data extraction logic (e.g., Gen 3 Contest stats) into separate, atomic stories for individual properties (Condition, Sheen, Ribbons) significantly reduces complexity and improves execution success during technical task planning.
- **2026-06-12 Pattern Insight:** When creating late-binding nodes (like `STORY` nodes for an active `EPIC`), we do not need to worry about circular dependency deadlocks caused by a child task depending on its `PENDING` parent. The orchestrator's new logic (late-binding logic exception for children) handles parent dependencies automatically, meaning a `PENDING` parent does not block its children from starting as long as it already has children.
- **2026-06-14 Pattern Insight:** As a Story Owner, I must not check off the acceptance criteria for child stories in the Epic markdown body until the entire spawned sub-tree of tasks is fully COMPLETED, to prevent premature verification. Submitting an empty PR while descendant tasks are still ACTIVE/PENDING will lead to Auditor rejection.
When breaking down an Epic into Stories, the Story Owner must ensure that the newly created Story's ID incorporates the exact sequence number of the parent Epic according to the Parent-Linked ID Schema (`<type>-<parent_NNN>-<NNN>-<slug>`). Additionally, the `owner_persona` of the generated Story must be assigned to the `tech_lead` to handle the technical implementation planning, not kept as `story_owner`.
- **2026-06-18 Pattern Insight:** When a child story (e.g., `story-081-122-gen3-rtc-extraction`) is cancelled due to technical impossibility or changes in architectural strategy (e.g., ADR 025), the Story Owner must create a new replacement story that aligns with the updated architectural decisions (e.g., `story-081-144-gen3-rtc-fallback-strategy`). The CANCELLED story must be checked off (`[x]`) in the parent Epic's markdown body, and the new story appended as an unchecked task (`[ ]`), without modifying the Epic's YAML frontmatter.

- **Pattern Insight:** When verifying macro nodes like EPICs, it's critical to recursively check that all spawned descendant nodes (down to the TASK level) have fully transitioned to the COMPLETED state before submitting an empty PR. Relying solely on the parent node's acceptance criteria checkboxes or immediate child nodes can prematurely transition the node to VERIFYING, leading to system inconsistency as the actual implementation might not yet be merged into the codebase. This applies to all deep levels of the spawned sub-tree.

- **2026-06-18 Pattern Insight:** When an Auditor rejects an Epic because previous implementation missed PRD requirements (e.g. only parsing counts instead of records), new complementary Stories must be spawned to address the missing scope. These new stories must be appended as unchecked tasks to the Epic without modifying its YAML frontmatter.
