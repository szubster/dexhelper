# 2026-09-13 11:25:00

- **Execution Plan Groundedness (Types & Schemas)**: Do not assume the properties or structure of TypeScript interfaces and database models (e.g., `PokemonMetadata`, `MoveMetadata`). You must explicitly locate and read their definitions in the trace (e.g., via `grep` or `cat src/db/schema.ts`) before referencing their properties in an execution plan.
- **Execution Plan Groundedness (Import Paths)**: Before proposing file creation commands that include `import` statements, you must strictly verify the exact file where the target hooks, functions, or components are exported from. Assuming a location (e.g., `../store` instead of `../hooks`) without trace verification will trigger a plan rejection.
- **Coding Constraints (Biome Accessibility)**: To satisfy strict accessibility linting rules (Biome), all `<label>` elements in React components must explicitly define an `htmlFor` attribute that strictly maps to the `id` of their associated input or control element.
- **Frontend Verification (Zustand State Injection)**: When writing temporary Playwright scripts to visually verify UI components that depend on application state, you can seamlessly mock the required state context by injecting data directly into the global window store using `page.evaluate('window.useStore.setState({ ... })')`.
