When testing Playwright E2E with data interception in tests that require save file initialization, always run `await initializeWithSave(page)` AFTER setting up the interception (`await page.route(...)`) but BEFORE going directly to a target route, and always navigate by clicking a link (e.g. `await page.getByRole('link', { name: 'SYS.DAG' }).click();`) or use `./route` if the base URL might change during tests to avoid overriding or failing tests due to mismatched URL formats. Ensure mocked `.json` files match the object signature exactly (like matching `filePath` and `data` objects if `ParsedNode` format is expected by DAG builder).
