# Late Binding Exception: Missing Gen 3 Binary Fixtures

While attempting to implement Playwright E2E tests for the Gen 3 Roamer Dossier (task-360-419-gen3-roamer-e2e-impl), I encountered a blocker because there are no valid Gen 3 `.sav` fixtures in the repository that contain active roamer data. The provided dummy `emerald.sav` fixture is entirely zeroed out, failing structural checksums in `parseGen3`.

The `test-utils` helper `initializeWithSave` injects raw binary buffers directly into IndexedDB, triggering `parseSaveFile`. Because the mock file is invalid, it throws immediately, leaving the application state uninitialized and rendering the required Roamer component unreachable. Attempting to bypass the parser by evaluating the `window.__STORE__` to inject a programmatic `SaveData` mock fails because the underlying architecture either reloads or is fundamentally broken by the initial parse failure.

Therefore, I am suspending this task using the late-binding orchestrator pattern. I have generated a `RESEARCH` node (`research-419-452-gen3-roamer-e2e-late-binding-research`) to investigate if valid Gen 3 binary fixtures can be procured or if E2E UI testing can be safely delegated to `vitest-browser-react` (which already possesses these component-level tests). The task status is marked as `FAILED`.
