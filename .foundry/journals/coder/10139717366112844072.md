- In Playwright E2E tests, when calling `page.goto()`, you should use a relative path with a leading dot (e.g., `page.goto('./dag')`) instead of an absolute path (e.g., `page.goto('/dag')`) to ensure the test correctly accounts for any configured `baseURL` (such as GitHub Pages subdirectories like `/dexhelper/`).
