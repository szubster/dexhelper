# Session 6138510518711341216

**Task**: Integrate Bike Badges into Map UI (task-414-423-map-ui-bike-integration-impl)

**Findings**:
The task requires updating `RouteRadarController` and `MapUI` to display bike badges based on whether an area requires a Mach or Acro bike. While `parseBikeRequirements` logic exists in `src/engine/gen3/mapParsing/mapRequirements.ts`, it is unclear where this data is actually persisted or exposed to the frontend (e.g. if it should be added to `UnifiedLocation` in `src/db/schema.ts` or passed separately via `apiData`).

**Action Taken**:
Suspended the task using the late-binding pattern due to missing context. Spawned a `RESEARCH` node (`research-413-458-bike-requirements-data-location`) to investigate where the bike requirements parsed in `task-412-424-gen3-bike-data-struct-integration-impl` are stored. Added the research node to this task's `depends_on` list and updated the status to `FAILED`.
