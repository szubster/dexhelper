# Gen 3 TV Shows and Event Schedule Memory Offsets

This document outlines the memory offsets, structures, and behavior related to the TV broadcast system, Mix Record events, and upcoming scheduled events (e.g., Energy Guru sales, Lilycove Department Store sales) in Generation 3 save files (specifically Ruby, Sapphire, and Emerald), based on the `pret/pokeemerald` decompilation.

## 1. Upcoming Event Schedule (`PokeNews`)

The game maintains an array of `PokeNews` structs within `SaveBlock1` to manage upcoming events such as the Energy Guru sale in Slateport and the Clearance Sale at Lilycove Department Store.

- **Offset within `SaveBlock1`:** `0x2B50`
- **Count:** `16` (constant `POKE_NEWS_COUNT`)
- **Total Array Size:** `64` bytes (`0x40`)

### `PokeNews` Struct Definition

Each `PokeNews` struct is **4 bytes** in size.

| Offset | Type | Name | Description |
|---|---|---|---|
| `0x00` | `u8` | `kind` | The type of news event (e.g., Energy Guru, Game Corner). |
| `0x01` | `u8` | `state` | The current state of the news event. |
| `0x02` | `u16` | `dayCountdown`| Days remaining until the event state changes. |

### Constants

**Event Kinds (`kind`):**
- `0`: `POKENEWS_NONE`
- `1`: `POKENEWS_SLATEPORT` (Energy Guru sale)
- `2`: `POKENEWS_GAME_CORNER` (Game Corner special)
- `3`: `POKENEWS_LILYCOVE` (Department Store Rooftop clearance sale)
- `4`: `POKENEWS_BLENDMASTER` (Blend Master appearance)

**Event States (`state`):**
- `0`: `POKENEWS_STATE_INACTIVE`
- `1`: `POKENEWS_STATE_UPCOMING`
- `2`: `POKENEWS_STATE_ACTIVE`

**Countdown Behavior:**
When an event transitions from `INACTIVE` to `UPCOMING`, `dayCountdown` is typically initialized to `POKENEWS_COUNTDOWN` (`4`).

---

## 2. TV Broadcast Data (`TVShow`)

The game stores TV show broadcasts in an array of `TVShow` structures within `SaveBlock1`. This array contains both normal shows generated by the player's own game and shows inherited from other players via the "Mix Records" feature.

- **Offset within `SaveBlock1`:** `0x27CC`
- **Count:** `25` (constant `TV_SHOWS_COUNT`, which is `NUM_NORMAL_TVSHOW_SLOTS` [5] + `20`)
- **Total Array Size:** `900` bytes (`0x384`)

### `TVShow` Union/Struct Definition

The `TVShow` type is a union of various structs tailored to specific shows (e.g., `fanclubLetter`, `smartshopperShow`, `massOutbreak`), all of which share a common header and total exactly **36 bytes (`0x24`)**.

The common header is:

| Offset | Type | Name | Description |
|---|---|---|---|
| `0x00` | `u8` | `kind` | The ID of the TV show broadcast. |
| `0x01` | `bool8` | `active` | Boolean flag indicating if the show is actively in rotation. |

Following the header, bytes `0x02` to `0x23` contain show-specific payload data, such as player names, words for the Easy Chat system, species IDs, and trainer IDs.

---

## 3. Parsing Mix Record Events

To identify events that originated from other players via **Mix Records**, you must iterate through the `tvShows` array and check the `kind` field of each active show.

TV shows are categorized into three logical groups by their `kind` ID:
- **Normal Shows** (1 - 20): Standard single-player shows.
- **Mix Record Shows** (21 - 40): Shows transferred between games.
- **Outbreak Shows** (41 - 60): Mass outbreak announcements.

### Relevant Mix Record `kind` IDs (`TVGROUP_RECORD_MIX`):
Shows with IDs between `TVGROUP_RECORD_MIX_START` (`21`) and `TVGROUP_RECORD_MIX_END` (`40`) represent actions performed by other players. Some notable IDs include:

- `21`: `TVSHOW_POKEMON_TODAY_CAUGHT`
- `22`: `TVSHOW_SMART_SHOPPER` (Details purchases from Poke Marts)
- `23`: `TVSHOW_POKEMON_TODAY_FAILED`
- `24`: `TVSHOW_FISHING_ADVICE`
- `25`: `TVSHOW_WORLD_OF_MASTERS`
- `31`: `TVSHOW_SECRET_BASE_VISIT`
- `33`: `TVSHOW_BATTLE_SEMINAR`

By parsing these specific shows, the system can extract the original player's name and details of their actions, effectively acting as an asynchronous multiplayer history log.

### `TVSHOW_MASS_OUTBREAK` Payload
When the `kind` field of a `TVShow` struct indicates a mass outbreak event (e.g. ID `TVSHOW_MASS_OUTBREAK` or another appropriate outbreak constant), the remaining bytes are structured as follows:

| Offset | Type | Name | Description |
|---|---|---|---|
| `0x00` | `u8` | `kind` | The ID of the TV show broadcast (common header). |
| `0x01` | `bool8` | `active` | Boolean flag indicating if the show is actively in rotation (common header). |
| `0x02` | `u8` | `unused1` | Unused byte. |
| `0x03` | `u8` | `unused3` | Unused byte. |
| `0x04` | `u16[4]` | `moves` | The 4 moves the mass outbreak Pokémon will have. |
| `0x0C` | `u16` | `species` | The internal species ID of the swarming Pokémon. |
| `0x0E` | `u16` | `unused2` | Unused half-word. |
| `0x10` | `u8` | `locationMapNum` | The Map Num where the outbreak occurs. |
| `0x11` | `u8` | `locationMapGroup` | The Map Group where the outbreak occurs. |
| `0x12` | `u8` | `unused4` | Unused byte. |
| `0x13` | `u8` | `probability` | Probability factor for the outbreak. |
| `0x14` | `u8` | `level` | The level of the swarming Pokémon. |
| `0x15` | `u8` | `unused5` | Unused byte. |
| `0x16` | `u16` | `daysBeforeOutbreak` | Days remaining until the mass outbreak happens (if not already started). |
| `0x18` | `u8` | `language` | The language of the broadcast. |
| `0x19` | `padding` | `padding` | Padding bytes (struct total size 36 bytes / 0x24). |
