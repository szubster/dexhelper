name: "🕰️ Schedule: Shield"
on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:
permissions:
  contents: read
jobs:
  run_shield:
    uses: ./.github/workflows/foundry-scheduled-agent.yml
    with:
      persona: "shield"
    secrets: inherit
