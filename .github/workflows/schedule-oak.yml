name: "🕰️ Schedule: Oak"
on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:
permissions:
  contents: read
jobs:
  run_oak:
    uses: ./.github/workflows/foundry-scheduled-agent.yml
    with:
      persona: "oak"
    secrets: inherit
