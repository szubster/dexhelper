name: "🕰️ Schedule: Bolt"
on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:
permissions:
  contents: read
jobs:
  run_bolt:
    uses: ./.github/workflows/foundry-scheduled-agent.yml
    with:
      persona: "bolt"
    secrets: inherit
