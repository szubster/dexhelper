name: "🕰️ Schedule: Lens"
on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:
permissions:
  contents: read
jobs:
  run_lens:
    uses: ./.github/workflows/foundry-scheduled-agent.yml
    with:
      persona: "lens"
    secrets: inherit
