name: "🕰️ Schedule: Canvas"
on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:
permissions:
  contents: read
jobs:
  run_canvas:
    uses: ./.github/workflows/foundry-scheduled-agent.yml
    with:
      persona: "canvas"
    secrets: inherit
