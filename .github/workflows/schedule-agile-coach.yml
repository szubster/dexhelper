name: "🕰️ Schedule: Agile Coach"
on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:
jobs:
  run_agile_coach:
    uses: ./.github/workflows/foundry-scheduled-agent.yml
    with:
      persona: "agile_coach"
    secrets: inherit
