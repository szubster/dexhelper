name: "🕰️ Schedule: Agile Coach"
on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:
permissions:
  contents: read
jobs:
  run_agile_coach:
    uses: ./.github/workflows/foundry-scheduled-agent.yml
    with:
      persona: "agile_coach"
    secrets: inherit
