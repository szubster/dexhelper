name: "🕰️ Schedule: Nurse"
on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:
permissions:
  contents: read
jobs:
  run_nurse:
    uses: ./.github/workflows/foundry-scheduled-agent.yml
    with:
      persona: "nurse"
    secrets: inherit
