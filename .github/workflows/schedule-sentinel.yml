name: "🕰️ Schedule: Sentinel"
on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:
permissions:
  contents: read
jobs:
  run_sentinel:
    uses: ./.github/workflows/foundry-scheduled-agent.yml
    with:
      persona: "sentinel"
    secrets: inherit
