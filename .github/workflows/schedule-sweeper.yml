name: "🕰️ Schedule: Sweeper"
on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:
permissions:
  contents: read
jobs:
  run_sweeper:
    uses: ./.github/workflows/foundry-scheduled-agent.yml
    with:
      persona: "sweeper"
    secrets: inherit
