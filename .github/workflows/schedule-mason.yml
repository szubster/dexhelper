name: "🕰️ Schedule: Mason"
on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:
permissions:
  contents: read
jobs:
  run_mason:
    uses: ./.github/workflows/foundry-scheduled-agent.yml
    with:
      persona: "mason"
    secrets: inherit
