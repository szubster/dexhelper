{
  "name": "foundry-scripts",
  "version": "1.0.0",
  "description": "Foundry DAG orchestration scripts — run with Node 24 native TypeScript support.",
  "type": "module",
  "scripts": {
    "orchestrate": "npx tsx foundry-orchestrator.ts",
    "orchestrate:dry": "npx tsx foundry-orchestrator.ts --dry-run",
    "orchestrate:strict": "npx tsx foundry-orchestrator.ts --strict",
    "verify:refs": "npx tsx verify-dag-refs.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "gray-matter": "^4.0.3"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "vitest": "^4.1.7"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
