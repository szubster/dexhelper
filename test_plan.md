1. Create the file `src/engine/saveParser/gen3/mixedRecords/parser.ts` and implement the `parseGen3MixedRecordNPC` function, following the Struct of Arrays format.
2. Read the file `src/engine/saveParser/gen3/mixedRecords/parser.ts` to confirm it was written correctly.
3. Create the file `src/engine/saveParser/gen3/mixedRecords/parser.test.ts` with unit tests covering successful extraction and boundary cases.
4. Read the file `src/engine/saveParser/gen3/mixedRecords/parser.test.ts` to confirm it was written correctly.
5. Run `pnpm lint && pnpm test` to verify the new tests pass, ensure project health, and confirm no regressions are introduced.
6. Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
7. Submit the PR.
