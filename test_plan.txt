I will extract the repeating section header `h3` component that is used consistently across pokemon details subcomponents (`PokemonLocations.tsx`, `PokemonCatchProbability.tsx`, `PokemonEvolutions.tsx`, `PokemonCaughtDetails.tsx`) and `SettingsLegend.tsx`.

The structure is typically:
```tsx
<h3 className="flex items-center gap-2 font-black text-[10px] text-[color] uppercase tracking-[0.3em]">
  {icon} {title}
</h3>
```

I will create `src/components/SectionHeader.tsx` or similar (maybe `TacticalSectionHeader.tsx` or `SectionHeading.tsx`).
